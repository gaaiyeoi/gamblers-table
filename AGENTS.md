# AI 助手工作约束

本文件是给在此仓库工作的 AI 助手（CodeBuddy 等）的项目级硬性约束。请严格遵守，不要跳过。

## 🚫 禁止滥用 Web Browser / 浏览器预览

- **不要**反复启动、打开或滥用 Web Browser / 浏览器预览（`preview_url`、`puppeteer_*`、`agent-browser` 等）。
- 浏览器预览会严重浪费用户机器性能，**每次打开都会造成明显卡顿**，请默认视为"高成本操作"。
- 除非用户**明确要求**"打开浏览器看看效果 / 截图 / 预览"，否则一律不启动浏览器。

## ✅ 默认工作方式：直接读源码、直接改代码

- 绝大多数任务直接**阅读源码 + 分析 + 修改代码**即可完成，无需可视化预览。
- 改动前先阅读相关组件/文件的源码，理解上下文后再修改。
- 排查问题优先用 `git diff` 定位本次改动/回归，而不是靠浏览器反复验证。
- 能用静态分析、单测（Vitest）、构建（`npm run build`）验证的，就不要用浏览器。
- 需要运行服务时，用后台/非交互方式启动（如 `npm run dev` 静默运行），避免长时间占用。

## 🎯 决策顺序（优先级从高到低）

1. 读源码 + `git diff` 定位问题
2. 静态检查 / 单测 / 构建验证
3. 询问用户是否需要浏览器验证
4. 仅当用户明确要求时才启动浏览器

---

> 这条约束来自用户真实反馈：浏览器预览对用户机器性能伤害大。请把"少开浏览器"当作默认习惯，而非例外。
